# Package lang.temper.supportedBackends

<!-- The h1 name is specially interpreted by dokka -->

Allows enumerating backends in the supported set and allows
creating a [*Backend*][lang.temper.be.Backend] instance reflectively
given a backend id as a string.
