class Utils {
  public diffStrings(a: String, b: String): List<DiffResults> {
    return list();
  }
  public diff<T>(a: T, b: T, (T, T)->Boolean): List<DiffResults> {
    return list();
  }
}

class DiffResults {}

export let diffUtils = new Utils();
