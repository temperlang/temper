  @fn let `test//`.f;
  `test//`.f = fn f(ls__7 /* aka ls */: ListBuilder<Int32>, j__8 /* aka j */: Int32) /* return__6 */: Void {
    fn__9: do {
      var i__10 = j__8;
## To do `ls[i++]--`, first we need to get the index `i++`.
      do {
        let t#0;
## `t#0` lets us avoid multiple evaluation of `ls`.
        t#0 = ls__7;
        let t#1;
        t#1 = do {
          let postfixReturn#3 = i__10;
          i__10 = do_call_succ(postfixReturn#3);
          postfixReturn#3
        };
## Now, `t#1` has the post-incremented `i`.
        do {
## Reading the array.
          let postfixReturn#2 = do_call_get(t#0, t#1);
## Writing the array.  Same array and element.
          do_call_set(t#0, t#1, do_call_pred(postfixReturn#2));
## The result is what was read from the array beforehand.
          postfixReturn#2
        }
      };
## Not as much to do for pre-increment and pre-decreemnt.
## `--ls[ls[++i]]` is what we're handling here.
##
      do {
        let t#4;
## Again, we get the subject.  The subject is a simple name,
## but if it were `var`, reading it's property could have the
## side-effect of setting it.
        t#4 = ls__7;
        let t#5;
        t#5 = do_call_get(ls__7, i__10 = do_call_succ(i__10));
        do_call_set(t#4, t#5, do_call_pred(do_call_get(t#4, t#5)))
      };
    }
  };
