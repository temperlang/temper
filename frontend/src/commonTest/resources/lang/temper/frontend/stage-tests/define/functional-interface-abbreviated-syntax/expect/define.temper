  @typeDecl(MyFunction) @stay @functionalInterface let `test//`.MyFunction;
  `test//`.MyFunction = type (MyFunction);
  do {};
  MyFunction extends AnyValue;
  @fn @stay @fromType(MyFunction) let apply__2;
## No `this` parameter on functional interface apply methods.
  apply__2 = (@stay fn apply(x__3 /* aka x */: Int32) /* return__1 */: Boolean {
      fn__4: do {
        pureVirtual()
      }
  });
  interface(\word, \MyFunction, void, void, void, \concrete, false, @typeDefined(MyFunction) @stay fn {
      do {};
      do {}
  });
