# Module be

<!-- The h1 name is specially interpreted by dokka -->

Common support code for output-language specific backends which
are in the `be-*` subprojects where the `*` is an abbreviation
for the output language.
