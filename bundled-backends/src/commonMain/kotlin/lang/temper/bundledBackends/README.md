# Package lang.temper.supportedBackends

<!-- The h1 name is specially interpreted by dokka -->

Allows enumerating backends in the default supported set and allows
creating a *Backend* instance reflectively given a backend id as a
string.
