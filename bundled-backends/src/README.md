# Module bundled-backends

Expresses compile-time dependencies on implementations of backends
that need to be bundled with a Temper compiler build so that they may
be loaded as plugins when running the compiler.
